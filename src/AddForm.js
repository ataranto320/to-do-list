import React, { Component } from 'react';

class AddTodo extends Component {
    // render jsx template
    render(){
        return (
            <div>
                <form onSubmit={}>
                    <label>Add New Todo:</label>
                </form>
            </div>
        )
    }
}